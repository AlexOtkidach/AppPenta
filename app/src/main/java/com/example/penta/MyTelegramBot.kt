package com.example.penta

//import org.telegram.telegrambots.meta.api.methods.send.SendMessage
//import org.telegram.telegrambots.meta.api.objects.Update
//import org.telegram.telegrambots.meta.bots.TelegramLongPollingBot
//import org.telegram.telegrambots.meta.exceptions.TelegramApiException

//class MyTelegramBot : TelegramLongPollingBot() {

    //override fun onUpdateReceived(update: Update) {
        // Получение сообщения от пользователя
        //if (update.hasMessage() && update.message.hasText()) {
            // Обработка полученного сообщения
            //val messageText = update.message.text
            //val chatId = update.message.chatId

            // Отправка ответного сообщения
            //val message = SendMessage()
                //.setChatId(chatId)
                //.setText("Привет! Опишите вашу проблему.")

            //try {
              //  execute(message) // Отправка сообщения
           // } catch (e: TelegramApiException) {
              //  e.printStackTrace()
          //  }
      //  }
   // }

   // override fun getBotUsername(): String {
        // Уникальное имя вашего бота
   //     return "PentaRuBot"
  //  }

 //   override fun getBotToken(): String {
        // Токен вашего бота
 //       return "6864849678:AAFc1MudeAKnbdnqlcirB7nLpl7HHfKZaGk"
 //   }
//}
